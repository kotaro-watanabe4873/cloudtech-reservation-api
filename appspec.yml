version: 0.0
Resources:
  - TargetService:
      Type: AWS::ECS::Service
      Properties:
        TaskDefinition: "cloudtech-reservation-api-task:5"
        LoadBalancerInfo:
          ContainerName: "api-server"
          ContainerPort: 80
